{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Print Workspace Info",
            "type": "shell",
            "command": "echo",
            "args": [
                "workspaceFolder: ${workspaceFolder}",
                "Current file: ${file}"
            ],
            "problemMatcher": []
        }
    ]
}